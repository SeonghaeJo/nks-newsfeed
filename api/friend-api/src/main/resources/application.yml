server:
  port: 8085

spring:
  application:
    name: friend-api
  profiles:
    active: local
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: password
  data:
    redis:
      host: localhost
      port: 6370
  cloud:
    compatibility-verifier:
      enabled: false

logging:
  level:
    com.personal.nksnewfeed: DEBUG